header.header
    .header__column
        a(href=routes.home)    
            h1 #{siteName}
            i.fab.fa-youtube.header__column
        ul
            li
                a(href=routes.join) join
            li
                a(href=routes.login) Login
