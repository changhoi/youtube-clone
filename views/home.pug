extends layouts/main
include mixins/videoBlock


block contents
    .videos
        each video in db
            +videoBlock({
                id: video.id,
                title: video.title,
                views: video.views,
                videoFile: video.videoFile
            })